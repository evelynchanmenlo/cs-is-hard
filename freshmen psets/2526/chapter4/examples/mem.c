// Pointers example by Evelyn Chan
#include <stdio.h>

int main(void)
{
    int x = 5;
    printf("The value of x is %i.\n", x);
    // the p in %p means pointer, and &x tells you the memory location
    printf("The address of x is %p.\n", &x);
}

