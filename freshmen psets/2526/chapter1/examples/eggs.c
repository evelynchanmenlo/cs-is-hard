// Eggs by Evelyn Chan
#include <cs50.h>
#include <stdio.h>

int main(void)
// command + / temporarily deactivates your code so it helps you with debugging (makes it into a comment)
{
    // int eggs = get_int("How many eggs do you have? ");
    // printf("Ah, so you have about %0.1f dozen eggs.\n", eggs / 12.0)
    // how to create a repeat ten in scratch (prints ten hello worlds)
    int i = 0;
    while (i < 10)
    {
        printf("Hello, world!\n");
        // means add 1 to i
        i = i+1;
    }
}
